DATABASE_URL="file:./data/dev.db"

# S3 / MinIO — stub until the image pipeline is wired up.
# Start MinIO via docker-compose.dev.yml and create a bucket named "kept-photos".
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="kept-photos"
